'use strict';

//
// sample codes START
//

// console.log('/sekigae/lib/sekigae.js is loaded') // WORK

// function Sekigae() {
//     this.my_func1 = function() {
//         return 'piyopiyo';
//     };
// };

// Sekigae.prototype.my_func2 = function() {
//     return 'hogehoge';
// };

// function my_func3() {
//     return 'fugafuga pya-';
// };

// module.exports = Sekigae;
// module.exports.my_func3 = my_func3();

//
// sample codes END
//

function Sekigae() {};

Sekigae.prototype.getToday = function(difference) {
    let date = new Date();
    // add difference between server & your local TimeZone
    date.setHours(date.getHours() + difference);

    let year = date.getFullYear();

    // add 1 (this method returns 0 to 11)
    let month = date.getMonth() + 1;

    let day = date.getDate();

    // fix format of month, day
    month = (month < 10 ? '0' : '') + month;
    day = (day < 10 ? '0' : '') + day;

    // get Day Name as number
    let weeknum = date.getDay();

    // set Day Name list
    let dayName = ['日曜日', '月曜日', '火曜日', '水曜日', '木曜日', '金曜日', '土曜日']

    return year + '/' + month + '/' + day + ' ' + dayName[weeknum]
};

module.exports = Sekigae;